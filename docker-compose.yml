services:
  api:
    build: .
    container_name: jussi-aibot-api
    ports:
      - "8000:8080"
    environment:
      PORT: 8080
    volumes:
      - .:/app
    command: python -m uvicorn main:app --host 0.0.0.0 --port 8080 --reload
